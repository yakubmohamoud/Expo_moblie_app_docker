# 🚨 Node.js Not Installed

Before running the code, you need to install Node.js (which includes npm).

## ⬇️ Installation Steps

### Windows:

1. **Download Node.js**
   - Go to https://nodejs.org
   - Click "Download LTS" (Long Term Support version)
   - This will download an installer

2. **Run the Installer**
   - Open the downloaded `.msi` file
   - Click "Next" through the installer
   - Accept the license agreement
   - Keep default settings
   - Click "Install"

3. **Verify Installation**
   ```bash
   node --version    # Should show v18.x.x or higher
   npm --version     # Should show 9.x.x or higher
   ```

4. **Restart Terminal**
   - Close PowerShell/Command Prompt completely
   - Open a new PowerShell window
   - Try commands again

---

## After Node.js is Installed

Run these commands to start your app:

```bash
# Navigate to moblie folder
cd "c:\Users\yakub\OneDrive\Documents\Tailoring app\Expo_moblie_app_docker\moblie"

# Install dependencies (takes ~3-5 minutes)
npm install

# Start Expo dev server
npm start
```

Then scan the QR code with **Expo Go** app on your phone.

---

## ⏱️ Timeline

- **Node.js Installation**: 5 minutes
- **npm install**: 3-5 minutes  
- **Expo startup**: 30 seconds
- **App load in Expo Go**: 10-15 seconds

**Total time to see app running**: ~10-15 minutes

---

**Download Node.js now, then come back and we'll run the app!**
